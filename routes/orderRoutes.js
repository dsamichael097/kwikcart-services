const express = require('express');
const router = express.Router();
const OrderController = require('../controllers/order');

// //router.get("",UserController.fetchUsers);
// router.post("/",OrderController.generateOTP);
// router.put("/:otp",OrderController.verifyOTP);
// //router.put("/:sessionId",OrderController.placeOrder);
// router.get("/:sessionId",OrderController.generateQR);
// router.post("/:mobno",OrderController.checkOrder);
// //router.put("/:id",ProductController.updateProduct);
// //router.delete("/:id",ProductController.deleteProduct);

module.exports = router;